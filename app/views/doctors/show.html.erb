<h1><%= @doctor.name %></h1>
<h3><%= @doctor.department %></h3><br>

<h2>Appointments:</h2>
<% @doctor.appointments.each do |app| %>
  <h3><%= app.appointment_datetime.strftime("%B %e, %Y at %k:%M") %></h3>
  <p>Scheduled with patient: <%= link_to app.patient.name, app.patient %></p>
<% end %>
